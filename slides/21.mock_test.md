### Mock Test

```javascript
function findUser(email, password, cb) {
  this.findOne({email: email})
    .exec()
    .then(function checkValidUser(user) {
      if (!user) {
        var error = new Error('Not found user');
        return cb(error);
      }

      if (!bcrypt.compareSync(password, user.password)) {
        var error = new Error('Not found user');
        return cb(error);
      }
      return cb(null, user._id.toString());
    })
    .catch(function (error) {
      error.status = 500;
      return cb(error);
    });
  };
```